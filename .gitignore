# Dépendances
node_modules/

# Builds
dist/
.next/
out/
*.tsbuildinfo
# Généré par `next typegen` / `next dev` / `next build`
apps/web/next-env.d.ts

# Environnement — .env.example est versionné, .env ne l'est jamais
.env
.env.*
!.env.example

# Logs
*.log
npm-debug.log*

# Éditeurs / OS
.idea/
.vscode/
.DS_Store

# Couverture de tests
coverage/

# Client Prisma généré (`prisma generate`) — reconstruit depuis le schéma
apps/api/src/generated/
