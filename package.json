{
  "name": "fripstock",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Outillage de dépôt pour Fripstock (convention de commit, formatage, release). Les applications vivent dans apps/ et ont leurs propres dépendances.",
  "scripts": {
    "commitlint": "commitlint",
    "prettier": "prettier"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.2.2",
    "@commitlint/config-conventional": "^21.2.2",
    "prettier": "^3.9.6"
  }
}
