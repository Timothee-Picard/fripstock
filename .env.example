# Copier ce fichier en `.env` avant le premier `make up` :
#   cp .env.example .env
# Valeurs de développement uniquement — ne jamais réutiliser telles quelles ailleurs.

# --- PostgreSQL ---
POSTGRES_USER=fripstock
POSTGRES_PASSWORD=fripstock
POSTGRES_DB=fripstock
POSTGRES_PORT=5432

# --- MinIO (stockage des photos produit) ---
MINIO_ROOT_USER=fripstock
MINIO_ROOT_PASSWORD=fripstock123
MINIO_PORT=9000
MINIO_CONSOLE_PORT=9001
MINIO_BUCKET=produits

# --- API ---
API_PORT=3001
JWT_SECRET=dev-secret-a-remplacer

# --- Web ---
WEB_PORT=3000
